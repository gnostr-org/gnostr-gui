name: build

# Controls when the action will run.
on:
  pull_request:
    branches:
      - '*'
      - '*/*'
      - '**'
      - 'master'
      - 'main'
  push:
    branches:
      - '*'
      - '*/*'
      - '**'
      - 'master'
      - 'main'

  workflow_dispatch:

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        os:
          - ubuntu-latest
          - macos-latest
          - windows-latest
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v3
      - uses: dtolnay/rust-toolchain@master
        with:
          toolchain: stable
      - uses: Swatinem/rust-cache@v2
      - run: cargo clean
      - run: cargo build --release
      - run: cat Cargo.lock
      - run: ls ./target/release/
      - if: matrix.os == 'windows-latest'
        uses: actions/upload-artifact@v3
        with:
          path: target/release/gnostr*.exe
          name: gnostr_gossip.exe
      - if: matrix.os == 'ubuntu-latest'
        uses: actions/upload-artifact@v3
        with:
          path: target/release/gnostr*
          name: gossip_linux
      - if: matrix.os == 'macos-latest'
        uses: actions/upload-artifact@v3
        with:
          path: target/release/gnostr*
          name: gossip_macos
